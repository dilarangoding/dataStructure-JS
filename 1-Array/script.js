let fruits = ["grapes", "star fruit", "cherry", "orange"];

// menghapus / menambah array di index terakhir
// fruits.push("coconut");
// fruits.pop();

// menghapus / menambah array di index pertama

// fruits.unshift('apel');
// fruits.shift();

/**
 * splice
 * first parameter where new elements should be added (spliced in)
 * second parameter how many elements should  be removed
 */
// fruits.splice(2, 2)

// Slice

const newArr = fruits.slice(2);
console.log(newArr);


console.log(fruits);

